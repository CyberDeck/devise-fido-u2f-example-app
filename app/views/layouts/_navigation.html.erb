<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed- top">
  <%= link_to render('layouts/logo'), root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><%= link_to 'Home', root_path, class: 'nav-link' %></li>
    </ul>
    <ul class="navbar-nav">
    <% if not signed_in? %>
      <li class="nav-item nav-item-btn"><%= link_to 'Sign in', new_user_session_path(), class: 'btn btn-outline-primary' %></li>
      <li class="nav-item nav-item-btn"><%= link_to 'Register', new_user_registration_path(), class: 'btn btn-outline-secondary' %></li>
    <% else %>
      <li class="nav-item mr-3"><%= link_to gravatar_for(current_user, size: 38, class: "rounded-circle"), edit_user_registration_path() %></li>
      <li class="nav-item nav-item-btn"><%= link_to 'Sign out', destroy_user_session_path(), method: 'delete', class: 'btn btn-outline-primary' %></li>
    <% end %>
    </ul>
  </div>
</nav>
