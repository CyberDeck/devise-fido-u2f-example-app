      <%= render 'navigation_for_login_or_registration', log_in: local_assigns[:log_in] %>
      <div class="tab-content p-2 border border-top-0" id="tabSignRegisterContent">
        <div class="tab-pane fade<%= " show active" if local_assigns[:log_in] %>" id="log-in" role="tabpanel">
          <%= render 'devise/sessions/form' %>
        </div>
        <div class="tab-pane fade<%= " show active" if !local_assigns[:log_in] %>" id="register" role="tabpanel">
          <%= render 'devise/registrations/form' %>
        </div>
      </div>
